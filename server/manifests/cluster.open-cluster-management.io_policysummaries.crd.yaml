---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.9.2
  creationTimestamp: null
  name: policysummaries.cluster.open-cluster-management.io
spec:
  group: cluster.open-cluster-management.io
  names:
    kind: PolicySummary
    listKind: PolicySummaryList
    plural: policysummaries
    shortNames:
    - plcs
    singular: policysummary
  scope: Cluster
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: PolicySummarySpec defines the desired state of PolicySummary
            type: object
          status:
            description: PolicySummaryStatus defines the observed state of PolicySummary
            properties:
              compliant:
                default: 0
                description: 'INSERT ADDITIONAL STATUS FIELD - define observed state
                  of cluster Important: Run "make" to regenerate code after modifying
                  this file'
                format: int32
                type: integer
              noncompliant:
                default: 0
                format: int32
                type: integer
              regionalHubs:
                items:
                  properties:
                    compliant:
                      default: 0
                      format: int32
                      type: integer
                    name:
                      type: string
                    noncompliant:
                      default: 0
                      format: int32
                      type: integer
                  required:
                  - compliant
                  - noncompliant
                  type: object
                type: array
            required:
            - compliant
            - noncompliant
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
